on: push
name: Build Zola
jobs:
  build:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: checkout
        uses: actions/checkout@v3.0.0
      - name: install
        uses: taiki-e/install-action@v2
        with:
          tool: zola@0.17.1
      - name: build
        run: zola build
        
      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
         local-dir: ./public/
         server: home29705826.1and1-data.host
         username: u1015849597
         password: ${{ secrets.PASSWORD }}
