name: Zola on GitHub Pages

on: 
 push:
  branches:
   - main

jobs:
  build:
    name: Publish site
    runs-on: ubuntu-latest
    steps:
    - name: Checkout main
    - uses: taiki-e/install-action@v2
      with:
       tool: zola@0.17.2
       run: zola build
      
